[workspace]
members = [
    "bootstrap",
    "devicetree",
    "interrupt",
    "kernel",
    "memory",
    "pl011",
    "runtime",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

